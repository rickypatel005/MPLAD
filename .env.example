# =============================================================================
# SIH26102 — MPLADS Audit Intelligence Unified Environment Configuration
# =============================================================================

# 1. DATABASE (PostgreSQL 15 + PostGIS 3.3)
DATABASE_URL="postgresql://mplads_admin:sih26102_secure_pwd@localhost:5432/mplads_audit"
POSTGRES_HOST="localhost"
POSTGRES_PORT=5432
POSTGRES_DB="mplads_audit"
POSTGRES_USER="mplads_admin"
POSTGRES_PASSWORD="sih26102_secure_pwd"
PG_MAX_CONNECTIONS=20

# 2. AUTHENTICATION & SECURITY (Phase 9)
JWT_SECRET="replace-with-a-unique-32-byte-secret"

# 3. AI / GEMINI FORENSIC ASSISTANT (Optional)
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# 4. BACKEND SERVER CONFIGURATION
PORT=3000
NODE_ENV="development"

# 5. FRONTEND CONFIGURATION (Person 4 Next.js HUD)
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000/api"
NEXT_PUBLIC_DEFAULT_ANONYMIZE=true
PORT_FRONTEND=3001
